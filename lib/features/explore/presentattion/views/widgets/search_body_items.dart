import 'package:flutter/material.dart';

class SearchBodyItems extends StatelessWidget {
  const SearchBodyItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}